# Workflow presentation-target capability inventory

Scope: current source in `bb-workflow-details`. Production usage is unmeasured because this environment has no production analytics connection; the current hot-reload trial is direct behavioral evidence, not a production count.

| Capability                                                                          | Status                     | Code reference                                                                                       | Gating path                                                                                                   | Prod usage | Evidence                                                                                                                     |
| ----------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------- |
| Agent-tool launch binds execution to the caller and resolves presentation ownership | `implemented-not-deployed` | `plugins/workflows/src/server.ts`, `plugins/workflows/src/service.ts`                                | `bb_workflow_run.execute` -> `service.start` -> parent-run inheritance or nearest visible ancestor            | unmeasured | Top-level callers self-present; hidden workflow workers inherit the causal root presentation.                                |
| CLI launch supports an explicit presentation thread                                 | `implemented-not-deployed` | `plugins/workflows/src/cli.ts`, `plugins/workflows/src/service.ts`                                   | `--present-in` -> visible same-project/same-environment ancestor validation -> `service.start`                | unmeasured | Explicit presentation overrides automatic selection while execution remains bound to the CLI context thread.                 |
| Inline workflow progress card                                                       | `exists-and-used`          | `plugins/workflows/src/app.tsx`                                                                      | Message directive thread ID -> `workflowRunView({ threadId, runId })` -> origin-or-presentation authorization | unmeasured | Existing directives remain usable from the origin; root visibility is supplied by the composer banner and inspector.         |
| Active workflow composer banner/card                                                | `implemented-not-deployed` | `plugins/workflows/src/app.tsx`, `plugins/workflows/src/data.ts`                                     | Current composer thread -> `workflowActiveRuns` -> origin-or-presentation query                               | unmeasured | Related descendant-origin runs appear on the presentation thread with a Related marker and panel action.                     |
| Right-side workflow inspector                                                       | `implemented-not-deployed` | `plugins/workflows/src/app.tsx`, `plugins/workflows/src/server.ts`                                   | Panel thread ID -> origin-or-presentation read authorization -> run and checkpoint views                      | unmeasured | Presentation access is read-only; stop control remains at the origin, with provenance and navigation from the presentation.  |
| Run lookup and authorization recognize origin and presentation threads              | `implemented-not-deployed` | `plugins/workflows/src/data.ts`, `plugins/workflows/src/server.ts`                                   | `origin_thread_id = ? OR presentation_thread_id = ?`; unrelated threads throw                                 | unmeasured | Focused RPC tests cover presentation access and unrelated-thread denial.                                                     |
| Realtime run projection targets execution and presentation threads                  | `implemented-not-deployed` | `plugins/workflows/src/service.ts`                                                                   | deduplicated publish over origin and presentation thread IDs                                                  | unmeasured | Origin=self emits once; related runs emit once to each thread.                                                               |
| Completion notification targets the origin thread                                   | `exists-and-used`          | `plugins/workflows/src/service.ts`                                                                   | `threads.send({ threadId: latest.originThreadId })`                                                           | unmeasured | Completion wakeup stays with the executing origin while realtime UI state also reaches the presentation thread.              |
| Workflow worker linkage                                                             | `exists-and-used`          | `plugins/workflows/src/service.ts`                                                                   | `getCallByChildThread(threadId)` -> owning run/call                                                           | unmeasured | The inspector links calls to worker threads, and worker-launched workflows inherit their owning run's causal presentation.   |
| Thread parent ancestry                                                              | `implemented-not-deployed` | `plugins/workflows/src/service.ts`                                                                   | hidden origins traverse `parentThreadId` with a cycle guard until the nearest visible ancestor                | unmeasured | The resolved ancestor is persisted as presentation ownership.                                                                |
| Separate human presentation thread                                                  | `implemented-not-deployed` | `plugins/workflows/src/data.ts`, `plugins/workflows/src/service.ts`                                  | durable `presentationThreadId` plus origin-or-presentation authorization                                      | unmeasured | Execution ownership, completion wakeup, and permissions stay with the origin thread.                                         |
| Ancestor/root aggregation of descendant-launched workflows                          | `implemented-not-deployed` | `plugins/workflows/src/data.ts`, `plugins/workflows/src/service.ts`, `plugins/workflows/src/app.tsx` | durable `parentRunId`/`rootRunId`, inherited presentation, and root-thread run projection                     | unmeasured | Surfaces related active cards and the latest related run inspector on the human-facing root without broad ancestry matching. |
| Cross-server workflow-card federation                                               | `does-not-exist`           | -                                                                                                    | -                                                                                                             | -          | Run storage and RPC lookup are local to one Workflows plugin/server; no federated run resolver was found.                    |

## Searches and evidence commands

No production usage query was available. Code inventory was produced with:

```bash
rg -n "workflow-preview|workflowActiveRuns|workflowRunView|originThreadId|threadPanelAction|getCallByChildThread" plugins/workflows/src apps/app/src
rg -n "parentThreadId|rootThreadId|ancestor" packages/plugin-sdk apps/server/src plugins/workflows/src
rg -n "workflowAllActiveThreadStatuses|experimental_threadHeaderAction" plugins/workflows/src apps/app/src/components/sidebar/ThreadRow.tsx
```

The final search found no current Workflows thread-header projection in this checkout.
